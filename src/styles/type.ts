/** **글자 계층** — 자리마다 크기·굵기를 따로 정하지 않고 **역할로 꺼내 쓴다** (2026-08-25).
 *
 *  ★★사용자 지적: *"각각을 개별로 폰트 스타일 지정하는 게 아니고 폰트 계층이 있고 거기서
 *    꺼내 써야 할 것 같다."* 실제로 같은 것이 자리마다 갈려 있었다 — 「카드 이름」이
 *    프롬프트에서는 13.8px/bold, 덱에서는 12px/semi, 씬에서는 12.8px/bold 였다.
 *    셋 다 같은 카드인데 화면마다 다른 물건으로 보였다.
 *
 *  ★크기 토큰(`--text-*`)·굵기 토큰(`--w-*`)은 **재료**다. 여기 있는 것은 **그 재료로 만든
 *    역할**이고, 화면은 역할만 고른다. 그래야 「카드 이름을 키우자」가 한 자리 수정이 된다.
 *  ★인라인 스타일로 쓰는 코드베이스라 클래스가 아니라 **객체**로 둔다 —
 *    `style={{ ...TYPE.cardName, color: … }}` 처럼 편다.
 *  ★역할을 새로 만들 때는 **이미 있는 것으로 안 되는가**를 먼저 답한다. 역할이 늘어나면
 *    계층이 아니라 목록이 되고, 그러면 지금과 같은 자리로 돌아온다.
 */
export const TYPE = {
  /** 묶음의 이름 — 그 안의 무엇보다 커야 한다 (`panels/Category`) */
  title: { fontSize: "var(--text-lg)", fontWeight: "var(--w-bold)" },

  /** ★★**카드 이름** — 프롬프트·씬·덱 **세 자리가 같은 것으로 읽혀야 한다.**
   *  ★굵기 상한(`--w-bold`)은 이 크기를 기준으로 잡은 값이다. 크기를 낮추면 그 상한이
   *    과해져 한글 획이 겹친다 (씬 카드에서 실제로 그랬다, 2026-08-25). */
  cardName: { fontSize: "0.86rem", fontWeight: "var(--w-bold)" },

  /** 라벨·탭 — 고를 수 있는 것의 이름 */
  label: { fontSize: "var(--text-xs)", fontWeight: "var(--w-semi)" },

  /** 읽는 글 (조수의 말·설명) */
  body: { fontSize: "var(--text-md)", fontWeight: "var(--w-normal)" },

  /** 곁들이는 값 — 수·시각·힌트 */
  meta: { fontSize: "var(--text-2xs)", fontWeight: "var(--w-normal)" },

  /** ★★**딱지** — 짧고 조용한 머리글 (`● CLI` · 「승인이 필요합니다」).
   *
   *  ★**자간이 역할에 들어 있다.** 이 크기는 자간이 있어야 읽힌다 — 크기만 꺼내 쓰면
   *    자리마다 자간을 또 적게 되고, 그러면 계층을 만든 뜻이 없다.
   *  ★긴 문장에 쓰지 말 것. 낱말 한둘짜리 머리글 자리다 — 설명·힌트는 `meta` 가 맡는다. */
  eyebrow: {
    fontSize: "var(--text-3xs)",
    fontWeight: "var(--w-normal)",
    letterSpacing: "0.06em",
  },
} as const;
