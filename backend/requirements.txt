# PeroPix 3.0 백엔드 의존성 (배포용 최소 구성)
# 로컬 엔진(torch/diffusers)은 3.0 에서 제거됐다.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
Pillow>=10.0.0
piexif>=1.1.3
aiofiles>=23.0.0
# ★파일 업로드(`/api/tools/meta` 의 `UploadFile`)에 필요하다. FastAPI 가 **부를 때가 아니라
#   임포트할 때** 없으면 죽으므로, 빠지면 백엔드가 아예 안 켜진다.
#   (2026-08-20 CI 가 잡았다 — 개발 기계에는 딸려 들어와 있어 안 보였다)
python-multipart>=0.0.9
websockets>=12.0

# 검열 — ★ONNX 로 번들한다 (사용자 결정 2026-08-05). ultralytics·torch 를 넣지 말 것:
#   torch 만 4.4GB 라 v2 는 "처음 켤 때 설치"를 골라야 했다. onnxruntime 은 그 100분의 1이다.
numpy>=1.26
# ★★실행기는 **플랫폼마다 다르다** (사용자 지적 2026-08-21: *"장당 5초씩 걸리면"*).
#   그때까지 CPU 판을 깔고 코드도 CPU 를 박아 써서, RTX 4080 이 있는 기계에서 XL 이
#   **4.8초/장**이었다. DirectML 판으로 바꾸니 **0.25초** — 같은 그림 16장에서 박스·점수까지
#   완전히 같았다 (실측 2026-08-21). v2 가 빨랐던 것도 같은 까닭이다 (torch 가 GPU 를 썼다).
#   ★DirectML 은 **윈도우 전용**이고 제조사를 안 가린다 (DX12 면 된다). 리눅스(CI)는 CPU 판.
#   ★고르는 것은 코드가 한다 — `censor.PREFER` 가 있는 것 중 앞에서부터 쓰고 CPU 로 떨어진다.
onnxruntime-directml>=1.17; sys_platform == "win32"
onnxruntime>=1.17; sys_platform != "win32"

# ★pywin32 는 **안 쓴다.** 탐색기 창을 앞으로 보내려고 한 번 넣었다가 걷었다 —
#   COM 으로 창을 훑는 방식이 눌릴 때마다 눈에 띄게 느렸다 (사용자 지적 2026-08-19).
#   지금은 `ctypes` 한 줄(`AllowSetForegroundWindow`)이라 의존성이 없다.
# ★NAI 생성 **스트리밍**의 프레임을 읽는다 (`nai.generate_streaming`).
#   공홈이 `parameters.stream = "msgpack"` 으로 보내는 길이 접두 msgpack 프레임이라,
#   직접 만들지 않고 검증된 디코더를 쓴다 (프레임에 그림 바이트가 들어 있다).
msgpack>=1.0.0
