node_modules/
dist/
logs/
src-tauri/target/
*.local
.DS_Store
__pycache__/

data/

workspaces/
qa-sandbox/share/

_tmp/

# ★★`reference/` 는 **임시가 아니다** — 다시 열어 볼 대조 원본(공홈 번들 등)이
#   사는 자리다 (사용자 결정 2026-08-21). 용량이 크고 배포물이 아니라 추적만 안 한다.
#   ★통째로 지우지 말 것 — git 사본이 없다 (`reference/README.md`).
reference/

# ── 개발 중에만 쓰는 것 (사용자 지시 2026-08-13) ──────────────────
# ★깃헙에 올라가는 것은 **앱 구동에 필요한 것뿐**이다. 아래는 디스크에 그대로 있고
#   추적만 안 한다 — 조사·목업·지침·테스트 하네스는 배포물이 아니라 **로컬 자료**다.
#   (저장소를 둘로 가르는 안은 접었다: 관리가 복잡해진다 — 사용자 결정 2026-08-13)
docs/
design/
gallery/
qa/
test/
_archive/
CLAUDE.md
.mcp.json
dev.bat
stop.bat
test.bat
tsconfig.tsbuildinfo
backend/test_*.py
*.test.ts
*.test.tsx

# ── 큰 검열 모델 (사용자 결정 2026-08-20) ───────────────────────────
# ★기본 모델(40MB)은 깃에 둔다 — 받은 사람이 바로 검열을 쓸 수 있어야 한다.
#   239MB 짜리는 **GitHub 파일 제한(100MB)을 넘어** 아예 올라가지 않는다.
#   코드는 원래 **가벼운 쪽을 기본**으로 고르므로(`backend/censor.py` 의 `models()`),
#   이 파일이 없어도 동작에 구멍이 없다 — 있으면 고를 수 있는 선택지가 하나 는다.
#   ★필요해지면 릴리즈 에셋으로 올리고 앱이 내려받게 한다 (LFS 는 무료 할당량이 작다).
models/censor/nsfw-anime-xl-x1280.onnx
